#!/usr/bin/python3
def uppercase(str):
    for i in str:
        if str>='a' and str<='z':
            result+=chr(ord(i) - ord('a') + ord('A')
        else:
            result+=str
    print(result)
